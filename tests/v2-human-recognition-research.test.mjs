import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const candidates=JSON.parse(fs.readFileSync('data/v2-research/human-recognition-candidates-v1.0.0.json','utf8'));
const real=JSON.parse(fs.readFileSync('data/v2-research/owner-retest-overlap-audit-v1.0.0.json','utf8'));
const simulation=JSON.parse(fs.readFileSync('data/v2-research/v5-retest-novelty-research-v1.0.0.json','utf8'));
test('human-recognition tranche is review-only and balanced',()=>{assert.equal(candidates.candidate_count,64);assert.equal(candidates.status,'PROPOSED_FOR_OWNER_REVIEW');assert.equal(candidates.production_authority,'NONE');assert.equal(new Set(candidates.candidates.map(q=>q.id)).size,64);for(const q of candidates.candidates){assert.equal(q.status,'REVIEW_ONLY');assert.equal(q.format,'SINGLE_SELECT');assert.equal(q.options.length,4);assert.deepEqual(new Set(q.options.map(o=>o.color)),new Set(['red','blue','yellow','green']));assert.ok(Object.values(q.quality).every(v=>v==='PASS'||v==='INSTANT_OR_EASY'||v==='REQUIRES_FINAL_BANK_RECONCILIATION'))}});
test('real-attempt evidence includes every governed bank generation',()=>{assert.equal(real.status,'READ_ONLY_FORENSIC_RESEARCH');assert.equal(real.attempts.length,6);assert.equal(real.consecutive_pair_count,5);for(const id of ['183-v1.0.0','293-v2.0.0','engagement-v4.0.0','expression-v5.0.0'])assert.ok(real.attempts.some(a=>a.bank.includes(id)));assert.equal(real.learning_item.id,'EXP2-S-16');assert.equal(real.learning_item.appearances.length,4)});
test('history-aware simulation preserves feasibility without runtime authority',()=>{assert.equal(simulation.status,'RESEARCH_ONLY');assert.equal(simulation.simulation.assembled_attempts,500);assert.equal(simulation.simulation.selector_failures,0);assert.equal(simulation.current_selector.participant_history_aware,false);assert.equal(simulation.simulation.history_aware_means.exact,0);assert.equal(simulation.simulation.history_aware_means.family,0)});
