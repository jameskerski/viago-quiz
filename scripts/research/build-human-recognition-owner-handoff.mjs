import fs from 'node:fs';const r=JSON.parse(fs.readFileSync('data/v2-research/human-recognition-final-review-v1.0.0.json','utf8')),s=JSON.parse(fs.readFileSync('data/v2-research/retest-novelty-assembler-v1-simulation.json','utf8'));const strongest=new Set([1,2,5,9,10,13,16,17,18,20,21,23,25,27,30,33,35,37,38,40,45,46,49,50,53,54,55,57,59,63].map(n=>`HR-S-${String(n).padStart(3,'0')}`));const list=d=>r.items.filter(q=>q.recommended_disposition===d).map(q=>q.id).join(', ')||'None';const full=q=>`### ${q.id}\n\n**${q.prompt}**\n\n${q.options.map(o=>`- ${o.label} — **${o.color.toUpperCase()}**`).join('\n')}\n`;
const replacements=r.items.filter(q=>q.recommended_disposition==='REPLACE_V5').map(q=>`- ${q.id} replaces \`${q.closest_current_v5.id}\`: “${q.closest_current_v5.prompt}”`).join('\n');const transitions=s.simulation.transitions.map(x=>`| ${x.from_attempt} → ${x.to_attempt} | ${x.mean_exact} | ${x.mean_family} | ${x.mean_construct} | ${x.mean_experiential} | ${x.mean_contexts} | ${x.mean_work} |`).join('\n');
const md=`# VIAGO Human Recognition V1 — final OWNER handoff\n\nResearch-only. No activation, deployment, scoring, selector-runtime, historical-attempt, or Production change.\n\n## A–F. Complete review and dispositions\n\nThe complete question/options/mapping/measurement/expression/human-recognition/closest-V5/disposition review is in [HUMAN_RECOGNITION_64_FINAL_OWNER_REVIEW.md](./HUMAN_RECOGNITION_64_FINAL_OWNER_REVIEW.md).\n\n- ADMIT_NEW: ${r.counts.ADMIT_NEW}\n- REPLACE_V5: ${r.counts.REPLACE_V5}\n- REVISE: ${r.counts.REVISE}\n- DEFER_REDUNDANCY: ${r.counts.DEFER_REDUNDANCY}\n- REJECT: ${r.counts.REJECT}\n\n**Rejected:** ${list('REJECT')}\n\n**Revise:** ${list('REVISE')}\n\n**Deferred:** ${list('DEFER_REDUNDANCY')}\n\n**Net-new:** ${list('ADMIT_NEW')}\n\n### Proposed V5 replacements\n\n${replacements}\n\n## G. Thirty strongest final human-recognition items\n\n${r.items.filter(q=>strongest.has(q.id)).map(full).join('\n')}\n## H. EXP2-S-16\n\nDisposition: **REBUILD_OR_REPLACE**. Proposed replacement: **HR-S-063**, which asks what makes someone stick with a new hobby and offers recognizable advancement, discovery, connection/meaning, and understanding instincts. The current environment prompt and its motivation/condition answers do not survive.\n\n## I. RETEST_NOVELTY_ASSEMBLER_V1 specification\n\n1. History window: three most recent completed attempts. An unfinished attempt contributes only question revisions actually answered; a zero-answer accidental start contributes nothing. Its own manifest remains immutable for resume.\n2. Immediately previous logical IDs are hard-excluded when the pool can meet all scoring/composition constraints.\n3. Revisions from the three-attempt window receive the strongest soft penalty after the immediate hard exclusion.\n4. Recent semantic families are excluded when feasible, then heavily penalized.\n5. Recent broad constructs are penalized; within-attempt family cap 1 and construct cap 3 remain.\n6. Fallback order: relax prior broad-construct penalty → older-family penalty → older-revision penalty → immediate-family penalty. Never relax composition, six-Likert/color, score opportunity, within-attempt identity uniqueness, attempt security, or immutable provenance. If the immediate-ID exclusion itself is impossible, record the exact relaxation and identities in the manifest; otherwise fail explicitly.\n7. Determinism key: bank ID/hash + assembler version + participant-history snapshot hash + seed. Persist the history IDs, policy version, penalty/relaxation diagnostics, question/option revisions, and final manifest hash.\n8. Historical versions resolve through immutable logical IDs and frozen metadata. Missing old family/construct metadata falls back conservatively to logical/revision avoidance; it is never guessed from rewritten text.\n9. A first attempt has an empty history snapshot and retains current quality/composition behavior.\n\n## J. Four-attempt simulation\n\n100 participants × four attempts, projected 305-item bank:\n\n| Transition | Exact | Family | Broad construct | Experiential screen | Contexts/attempt | Work items/attempt |\n|---|---:|---:|---:|---:|---:|---:|\n${transitions}\n\nFailures: ${s.simulation.selector_failures}/400. Each attempt retained 26 single-select, 24 Likert, six Likerts per color, equal 128-point theoretical opportunity, and deterministic replay. Experiential repetition uses a conservative prompt-token screen and remains a human-review signal, not semantic authority.\n\n## K–M. Projected bank and scoring integrity\n\nProjected bank: **${r.projected.final_count}** questions: **${r.projected.single_select} single-select / ${r.projected.likert} Likert**. This is a projection, not a target or frozen bank. Twenty candidates replace twenty V5 single-selects; thirteen are net-new. The attempted 50-question composition, option weights, tie behavior, scoring version, and 128-point opportunity per color are unchanged.\n\n## N. OWNER decisions required\n\n- Approve/reject each of the 20 replacement pairings and 13 net-new admissions.\n- Decide whether the 19 REVISE items should receive another writing pass or remain outside the bank.\n- Confirm HR-S-063 as the EXP2-S-16 replacement.\n- Approve the three-completed-attempt history window and answered-only handling of unfinished attempts.\n- Approve the explicit fallback hierarchy before implementation.\n\n## O. Recommendation\n\n**READY_FOR_OWNER_FINAL_BANK_REVIEW**\n`;
fs.writeFileSync('docs/v2/HUMAN_RECOGNITION_FINAL_OWNER_HANDOFF.md',md);console.log(JSON.stringify({strongest:strongest.size,counts:r.counts,projection:r.projected,recommendation:'READY_FOR_OWNER_FINAL_BANK_REVIEW'},null,2));
