NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_QUIZ_SCHEMA=public
# Optional until the owner chooses the historical result-link policy.
QUIZ_ATTEMPT_TOKEN_SECRET=
QUIZ_REQUIRE_ATTEMPT_TOKEN=false
# Set only during the brief final-delta window; /api/start and /api/answer return 503.
QUIZ_WRITE_FREEZE=false
